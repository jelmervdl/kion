{% extends 'admin/base.html.twig' %}

{% block body %}
<h2>Edit page</h2>
{{ form_start(form) }}
	{{ form_widget(form) }}
	<button type="submit" class="btn btn-primary">Submit</button>
{{ form_end(form) }}
{% endblock %}